name: api
description: A new brick created with the Mason CLI.
version: 0.1.1+2

environment:
  mason: ">=0.1.0-dev.51 <0.1.0"

vars:
  name:
    type: string
    description: API name
    prompt: Provide name for your API
